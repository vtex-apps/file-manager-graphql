type File {
  fileUrl: String!
  mimetype: String!
  encoding: String!
}

type Settings {
  maxFileSizeMB: Int
}

type Mutation {
  """Save file (returns file's immutable URL)"""
  uploadFile(file: Upload!, bucket: String = "images"): File @withAuthMetrics
  """Delete file from bucket"""
  deleteFile(path: String!, bucket: String = "images"): Boolean! @withAuthMetrics
}

type Query {
  """Get file’s immutable URL"""
  getFileUrl(path: String!, bucket: String = "images"): String @withAuthMetrics
  """Get file"""
  getFile(path: String!, width: Int = 1, height: Int = 2, aspect: Boolean = false, bucket: String = "images"): String @withAuthMetrics
  """Settings"""
  settings: Settings @withAuthMetrics
}
